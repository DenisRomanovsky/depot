<%  if notice %>
<p id="notice"><%= notice %></p>
<% end %>

<h2> Depot Cart </h2>

<ul>
  <% @cart.line_items.each do |item| %>
    <li> <%= item.quantity.to_s + " time".pluralize(item.quantity) + " " + item.product.title %> </li>
    <% end %>
</ul>

<%= link_to 'Back', store_path %>
<p> <%= session[:show_store_counter] %>  </p>
